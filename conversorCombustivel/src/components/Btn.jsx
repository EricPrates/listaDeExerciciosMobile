import { Button, StyleSheet} from "react-native";

export default function Btn(){

    return(
        <Button>Calcular qual é mais vantajoso</Button>
    );
}

const style = StyleSheet.create({
    btn: {
        borderRadius: 5,
        backgroundColor: '#'
    }
}
)